---
const t = {
  hero: { title: 'Анонимная социальная сеть без цензуры и слежки', sub: 'Общайтесь свободно. Без аккаунтов, рекламы и контроля.', cta1: 'Получить ранний доступ', cta2: 'Как это работает' },
  problem: { title: 'Проблема', items: [
    { icon: '👁', text: 'Сбор персональных данных' },
    { icon: '📍', text: 'Слежка и профилирование' },
    { icon: '🚫', text: 'Цензура контента' },
    { icon: '💔', text: 'Утечки данных' }
  ]},
  solution: { title: 'Решение', items: [
    { icon: '🔗', title: 'Peer-to-Peer', text: 'Прямое соединение между пользователями' },
    { icon: '🆔', title: 'Децентрализованные ID', text: 'Анонимная идентификация без регистрации' },
    { icon: '🔐', title: 'E2E шифрование', text: 'Только вы читаете свои сообщения' },
    { icon: '☁️', title: 'Нет серверов', text: 'Данные не хранятся централизованно' }
  ]},
  how: { title: 'Как это работает', steps: [
    { num: '01', title: 'Генерация ключа', text: 'Создайте анонимный криптографический ключ' },
    { num: '02', title: 'Подключение', text: 'Присоединитесь к P2P сети' },
    { num: '03', title: 'Общение', text: 'Обменивайтесь зашифрованными сообщениями' },
    { num: '04', title: 'Контроль', text: 'Только вы владеете своими данными' }
  ]},
  security: { title: 'Безопасность', tech: ['Curve25519', 'AES-256-GCM', 'Onion Routing', 'Zero-Knowledge'], no: ['Логов', 'Cookies', 'Трекеров', 'Аналитики'] },
  features: { title: 'Возможности', items: [
    { icon: '👤', title: 'Анонимные профили', text: 'Без email и телефона' },
    { icon: '👥', title: 'Приватные сообщества', text: 'Закрытые группы по интересам' },
    { icon: '💬', title: 'Личные сообщения', text: 'Полностью зашифрованные чаты' },
    { icon: '📰', title: 'Децентрализованный фид', text: 'Без алгоритмов и манипуляций' },
    { icon: '⭐', title: 'Репутация без ID', text: 'Доверие без раскрытия личности' },
    { icon: '🛡', title: 'Защита от спама', text: 'Proof-of-Work верификация' }
  ]},
  audience: { title: 'Для кого', items: [
    { icon: '✊', text: 'Активисты' },
    { icon: '📝', text: 'Журналисты' },
    { icon: '💻', text: 'Разработчики' },
    { icon: '🌍', text: 'Все, кто ценит приватность' }
  ]},
  roadmap: { title: 'Roadmap', items: [
    { phase: 'Q1', title: 'Alpha', status: 'active' },
    { phase: 'Q2', title: 'Beta', status: '' },
    { phase: 'Q3', title: 'Mobile', status: '' },
    { phase: 'Q4', title: 'DAO', status: '' }
  ]},
  cta: { title: 'Присоединяйтесь', sub: 'Станьте частью сети, где свобода — по умолчанию.', btn: 'Join Waitlist', success: 'Вы в списке!' },
  footer: { links: ['Whitepaper', 'GitHub', 'FAQ'], disclaimer: 'Nodus не хранит пользовательские данные. Privacy by design.' }
};
---
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="description" content="Nodus — децентрализованная анонимная социальная сеть без цензуры и слежки">
  <title>Nodus — Анонимная социальная сеть</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    *{margin:0;padding:0;box-sizing:border-box}
    :root{--bg:#050508;--surface:#0d0d12;--surface2:#14141b;--accent:#00ff88;--accent2:#8b5cf6;--text:#f4f4f5;--muted:#a1a1aa;--border:#1f1f28}
    html{scroll-behavior:smooth}
    body{font-family:'Inter',system-ui,sans-serif;background:var(--bg);color:var(--text);line-height:1.7;overflow-x:hidden}
    
    /* Animated background */
    .bg-grid{position:fixed;inset:0;background-image:linear-gradient(rgba(255,255,255,.02) 1px,transparent 1px),linear-gradient(90deg,rgba(255,255,255,.02) 1px,transparent 1px);background-size:60px 60px;pointer-events:none;z-index:-1}
    .bg-glow{position:fixed;width:600px;height:600px;border-radius:50%;filter:blur(120px);opacity:.15;pointer-events:none;z-index:-1}
    .bg-glow-1{top:-200px;left:-200px;background:var(--accent2)}
    .bg-glow-2{bottom:-200px;right:-200px;background:var(--accent)}
    
    .container{max-width:1200px;margin:0 auto;padding:0 2rem}
    section{padding:8rem 0;position:relative}
    
    /* Typography */
    h1{font-size:clamp(2.5rem,6vw,4.5rem);font-weight:700;line-height:1.1;letter-spacing:-.02em}
    h2{font-size:clamp(1.75rem,4vw,2.5rem);font-weight:600;margin-bottom:1rem;letter-spacing:-.01em}
    .gradient-text{background:linear-gradient(135deg,var(--accent) 0%,var(--accent2) 100%);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text}
    .section-label{display:inline-flex;align-items:center;gap:.5rem;font-size:.875rem;font-weight:500;color:var(--accent);text-transform:uppercase;letter-spacing:.1em;margin-bottom:1.5rem}
    .section-label::before{content:'';width:2rem;height:1px;background:var(--accent)}
    
    /* Navigation */
    nav{position:fixed;top:0;left:0;right:0;z-index:100;padding:1rem 0;backdrop-filter:blur(20px);background:rgba(5,5,8,.8);border-bottom:1px solid var(--border)}
    nav .container{display:flex;justify-content:space-between;align-items:center}
    .logo{font-size:1.25rem;font-weight:700;color:var(--accent);text-decoration:none;display:flex;align-items:center;gap:.5rem}
    .logo svg{width:28px;height:28px}
    .nav-links{display:flex;gap:2rem;align-items:center}
    .nav-links a{color:var(--muted);text-decoration:none;font-size:.875rem;transition:color .2s}
    .nav-links a:hover{color:var(--text)}
    .lang-switch{padding:.5rem 1rem;border:1px solid var(--border);border-radius:6px;background:transparent;color:var(--muted);font-size:.875rem;cursor:pointer;transition:all .2s}
    .lang-switch:hover{border-color:var(--accent);color:var(--accent)}
    
    /* Hero */
    .hero{min-height:100vh;display:flex;align-items:center;padding-top:6rem}
    .hero-content{max-width:800px}
    .hero-badge{display:inline-flex;align-items:center;gap:.5rem;padding:.5rem 1rem;background:rgba(139,92,246,.1);border:1px solid rgba(139,92,246,.2);border-radius:100px;font-size:.875rem;color:var(--accent2);margin-bottom:2rem}
    .hero-badge span{width:6px;height:6px;background:var(--accent2);border-radius:50%;animation:pulse 2s infinite}
    @keyframes pulse{0%,100%{opacity:1}50%{opacity:.4}}
    .hero p{font-size:1.25rem;color:var(--muted);margin:1.5rem 0 2.5rem;max-width:600px}
    .hero-btns{display:flex;gap:1rem;flex-wrap:wrap}
    .hero-visual{position:absolute;right:0;top:50%;transform:translateY(-50%);width:45%;max-width:500px;aspect-ratio:1;opacity:.6}
    .hero-visual::before{content:'';position:absolute;inset:0;border:1px solid var(--border);border-radius:50%;animation:rotate 30s linear infinite}
    .hero-visual::after{content:'';position:absolute;inset:15%;border:1px dashed var(--border);border-radius:50%;animation:rotate 20s linear infinite reverse}
    @keyframes rotate{to{transform:rotate(360deg)}}
    
    /* Buttons */
    .btn{display:inline-flex;align-items:center;gap:.5rem;padding:1rem 2rem;border-radius:12px;font-weight:600;font-size:.9375rem;text-decoration:none;transition:all .3s cubic-bezier(.4,0,.2,1);cursor:pointer;border:none}
    .btn-primary{background:var(--accent);color:var(--bg);box-shadow:0 0 30px rgba(0,255,136,.2)}
    .btn-primary:hover{transform:translateY(-2px);box-shadow:0 0 40px rgba(0,255,136,.3)}
    .btn-secondary{background:transparent;border:1px solid var(--border);color:var(--text)}
    .btn-secondary:hover{border-color:var(--accent);color:var(--accent)}
    .btn-icon{width:20px;height:20px}
    
    /* Cards */
    .grid{display:grid;gap:1.5rem}
    .grid-2{grid-template-columns:repeat(2,1fr)}
    .grid-3{grid-template-columns:repeat(3,1fr)}
    .grid-4{grid-template-columns:repeat(4,1fr)}
    @media(max-width:900px){.grid-3,.grid-4{grid-template-columns:repeat(2,1fr)}}
    @media(max-width:600px){.grid-2,.grid-3,.grid-4{grid-template-columns:1fr}}
    
    .card{background:var(--surface);border:1px solid var(--border);border-radius:16px;padding:2rem;transition:all .3s;position:relative;overflow:hidden}
    .card:hover{border-color:rgba(0,255,136,.3);transform:translateY(-4px)}
    .card::before{content:'';position:absolute;top:0;left:0;right:0;height:1px;background:linear-gradient(90deg,transparent,var(--accent),transparent);opacity:0;transition:opacity .3s}
    .card:hover::before{opacity:1}
    .card-icon{font-size:2rem;margin-bottom:1rem;display:block}
    .card h3{font-size:1.125rem;font-weight:600;margin-bottom:.5rem}
    .card p{color:var(--muted);font-size:.9375rem}
    
    /* Problem cards */
    .problem-card{background:linear-gradient(135deg,rgba(239,68,68,.05),transparent);border-color:rgba(239,68,68,.2)}
    .problem-card:hover{border-color:rgba(239,68,68,.4)}
    
    /* Steps */
    .steps-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:2rem;position:relative}
    .steps-grid::before{content:'';position:absolute;top:3rem;left:10%;right:10%;height:1px;background:linear-gradient(90deg,transparent,var(--border),var(--border),transparent)}
    .step{text-align:center;position:relative}
    .step-num{width:4rem;height:4rem;margin:0 auto 1.5rem;background:var(--surface2);border:2px solid var(--border);border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:1.25rem;font-weight:700;color:var(--accent);position:relative;z-index:1}
    .step h3{font-size:1rem;font-weight:600;margin-bottom:.5rem}
    .step p{color:var(--muted);font-size:.875rem}
    @media(max-width:800px){.steps-grid{grid-template-columns:repeat(2,1fr)}.steps-grid::before{display:none}}
    @media(max-width:500px){.steps-grid{grid-template-columns:1fr}}
    
    /* Security */
    .security-grid{display:grid;grid-template-columns:1fr 1fr;gap:3rem;align-items:start}
    .tech-list{display:flex;flex-wrap:wrap;gap:.75rem}
    .tech-tag{padding:.75rem 1.25rem;background:rgba(0,255,136,.08);border:1px solid rgba(0,255,136,.2);border-radius:8px;font-size:.875rem;font-weight:500;color:var(--accent)}
    .no-list{display:flex;flex-wrap:wrap;gap:.75rem}
    .no-tag{padding:.75rem 1.25rem;background:rgba(239,68,68,.08);border:1px solid rgba(239,68,68,.2);border-radius:8px;font-size:.875rem;font-weight:500;color:#f87171;text-decoration:line-through;text-decoration-color:rgba(248,113,113,.4)}
    @media(max-width:700px){.security-grid{grid-template-columns:1fr}}
    
    /* Audience */
    .audience-card{display:flex;align-items:center;gap:1rem;padding:1.5rem}
    .audience-card .card-icon{margin:0;font-size:1.5rem}
    
    /* Roadmap */
    .roadmap-track{display:flex;gap:0;position:relative;overflow-x:auto;padding:2rem 0}
    .roadmap-item{flex:1;min-width:200px;text-align:center;position:relative;padding:0 1rem}
    .roadmap-item::before{content:'';position:absolute;top:1.25rem;left:0;right:0;height:2px;background:var(--border)}
    .roadmap-item:first-child::before{left:50%}
    .roadmap-item:last-child::before{right:50%}
    .roadmap-dot{width:2.5rem;height:2.5rem;margin:0 auto 1rem;background:var(--surface2);border:2px solid var(--border);border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:.75rem;font-weight:700;color:var(--muted);position:relative;z-index:1}
    .roadmap-item.active .roadmap-dot{background:var(--accent);border-color:var(--accent);color:var(--bg)}
    .roadmap-item h4{font-size:1rem;font-weight:600;margin-bottom:.25rem}
    .roadmap-item span{font-size:.75rem;color:var(--muted);text-transform:uppercase;letter-spacing:.05em}
    
    /* CTA Section */
    .cta-section{text-align:center;background:linear-gradient(180deg,var(--surface) 0%,var(--bg) 100%);border:1px solid var(--border);border-radius:24px;padding:5rem 3rem;position:relative;overflow:hidden}
    .cta-section::before{content:'';position:absolute;top:-50%;left:-50%;width:200%;height:200%;background:radial-gradient(circle,rgba(0,255,136,.03) 0%,transparent 50%);pointer-events:none}
    .cta-section h2{margin-bottom:.5rem}
    .cta-section>p{color:var(--muted);margin-bottom:2.5rem;font-size:1.125rem}
    #waitlist-form{display:flex;gap:.75rem;max-width:450px;margin:0 auto;flex-wrap:wrap;justify-content:center}
    
    /* Footer */
    footer{padding:3rem 0;border-top:1px solid var(--border)}
    footer .container{display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:1.5rem}
    .footer-links{display:flex;gap:2rem}
    .footer-links a{color:var(--muted);text-decoration:none;font-size:.875rem;transition:color .2s}
    .footer-links a:hover{color:var(--accent)}
    footer p{color:var(--muted);font-size:.8125rem}
    
    /* Animations */
    @keyframes fadeUp{from{opacity:0;transform:translateY(30px)}to{opacity:1;transform:translateY(0)}}
    .animate{animation:fadeUp .6s ease-out forwards;opacity:0}
    .delay-1{animation-delay:.1s}
    .delay-2{animation-delay:.2s}
    .delay-3{animation-delay:.3s}
    
    @media(max-width:900px){
      .hero-visual{display:none}
      section{padding:5rem 0}
      .nav-links{display:none}
    }
  </style>
</head>
<body>
  <div class="bg-grid"></div>
  <div class="bg-glow bg-glow-1"></div>
  <div class="bg-glow bg-glow-2"></div>

  <nav>
    <div class="container">
      <a href="/" class="logo">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/><path d="M2 12h20"/></svg>
        Nodus
      </a>
      <div class="nav-links">
        <a href="#how">Как работает</a>
        <a href="#features">Возможности</a>
        <a href="#security">Безопасность</a>
        <a href="/en/" class="lang-switch">EN</a>
      </div>
    </div>
  </nav>

  <section class="hero">
    <div class="container">
      <div class="hero-content">
        <div class="hero-badge animate"><span></span> Ранний доступ открыт</div>
        <h1 class="gradient-text animate delay-1">{t.hero.title}</h1>
        <p class="animate delay-2">{t.hero.sub}</p>
        <div class="hero-btns animate delay-3">
          <a href="#waitlist" class="btn btn-primary">{t.hero.cta1}</a>
          <a href="#how" class="btn btn-secondary">{t.hero.cta2} →</a>
        </div>
      </div>
    </div>
    <div class="hero-visual"></div>
  </section>

  <section>
    <div class="container">
      <span class="section-label">Проблема</span>
      <h2>Почему нужна альтернатива</h2>
      <div class="grid grid-4" style="margin-top:2rem">
        {t.problem.items.map(item => (
          <div class="card problem-card">
            <span class="card-icon">{item.icon}</span>
            <p>{item.text}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <section>
    <div class="container">
      <span class="section-label">Решение</span>
      <h2 class="gradient-text">Архитектура свободы</h2>
      <div class="grid grid-2" style="margin-top:2rem">
        {t.solution.items.map(item => (
          <div class="card">
            <span class="card-icon">{item.icon}</span>
            <h3>{item.title}</h3>
            <p>{item.text}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <section id="how">
    <div class="container">
      <span class="section-label">Процесс</span>
      <h2>Как это работает</h2>
      <div class="steps-grid" style="margin-top:3rem">
        {t.how.steps.map(step => (
          <div class="step">
            <div class="step-num">{step.num}</div>
            <h3>{step.title}</h3>
            <p>{step.text}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <section id="security">
    <div class="container">
      <span class="section-label">Защита</span>
      <h2>Безопасность и приватность</h2>
      <div class="security-grid" style="margin-top:2rem">
        <div>
          <h3 style="margin-bottom:1rem;color:var(--accent)">Используемые технологии</h3>
          <div class="tech-list">
            {t.security.tech.map(item => <span class="tech-tag">{item}</span>)}
          </div>
        </div>
        <div>
          <h3 style="margin-bottom:1rem;color:#f87171">Чего у нас нет</h3>
          <div class="no-list">
            {t.security.no.map(item => <span class="no-tag">{item}</span>)}
          </div>
        </div>
      </div>
    </div>
  </section>

  <section id="features">
    <div class="container">
      <span class="section-label">Функции</span>
      <h2>Возможности платформы</h2>
      <div class="grid grid-3" style="margin-top:2rem">
        {t.features.items.map(item => (
          <div class="card">
            <span class="card-icon">{item.icon}</span>
            <h3>{item.title}</h3>
            <p>{item.text}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <section>
    <div class="container">
      <span class="section-label">Аудитория</span>
      <h2>Для кого Nodus</h2>
      <div class="grid grid-4" style="margin-top:2rem">
        {t.audience.items.map(item => (
          <div class="card audience-card">
            <span class="card-icon">{item.icon}</span>
            <p style="font-weight:500">{item.text}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <section>
    <div class="container">
      <span class="section-label">План</span>
      <h2>Roadmap</h2>
      <div class="roadmap-track">
        {t.roadmap.items.map(item => (
          <div class={`roadmap-item ${item.status}`}>
            <div class="roadmap-dot">{item.phase}</div>
            <h4>{item.title}</h4>
          </div>
        ))}
      </div>
    </div>
  </section>

  <section id="waitlist">
    <div class="container">
      <div class="cta-section">
        <span class="section-label">Присоединяйтесь</span>
        <h2 class="gradient-text">{t.cta.title}</h2>
        <p>{t.cta.sub}</p>
        <div id="waitlist-form">
          <a href="https://t.me/NodusSocial" class="btn btn-primary" target="_blank">{t.cta.btn}</a>
        </div>
      </div>
    </div>
  </section>

  <footer>
    <div class="container">
      <div class="footer-links">
        {t.footer.links.map(link => <a href="#">{link}</a>)}
      </div>
      <p>{t.footer.disclaimer}</p>
    </div>
  </footer>

  <script>
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(e => { if(e.isIntersecting) e.target.classList.add('animate') });
    }, { threshold: 0.1 });
    document.querySelectorAll('.card, .step, .roadmap-item').forEach(el => observer.observe(el));
  </script>
</body>
</html>
